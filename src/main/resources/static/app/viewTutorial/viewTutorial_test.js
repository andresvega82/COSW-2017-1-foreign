'use strict';

describe('myApp.viewTutorial module', function() {

    beforeEach(module('myApp.viewTutorial'));

    describe('viewTutorial controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('ViewTutorialCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});